{
  "name": "statusline-desktop",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "npm@11.17.0",
  "scripts": {
    "dev": "vite",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "prettier --write \"src/**/*.{ts,css}\" \"scripts/**/*.mjs\" \"src-tauri/tauri*.json\" ../.github/workflows/desktop-installers.yml ../docs/release/desktop-installers.md index.html package.json tsconfig.json vite.config.ts README.md",
    "format:check": "prettier --check \"src/**/*.{ts,css}\" \"scripts/**/*.mjs\" \"src-tauri/tauri*.json\" ../.github/workflows/desktop-installers.yml ../docs/release/desktop-installers.md index.html package.json tsconfig.json vite.config.ts README.md",
    "build": "npm run check && vite build",
    "release:preflight": "node scripts/check-release.mjs",
    "release:check": "npm run release:preflight && npm run format:check && npm test && npm run check",
    "bundle:windows": "tauri build --ci --bundles nsis,msi",
    "bundle:linux": "tauri build --ci --bundles deb,rpm,appimage",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "2.11.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4",
    "prettier": "3.9.6",
    "typescript": "7.0.2",
    "vite": "8.2.2",
    "vitest": "4.1.11"
  }
}
