Build 1.0.1 (5): independent widget synchronization and privacy/support links on the official website.

Upgrade an already paired iPhone and open Statusline once to migrate the shared reader credential. Confirm pairing and quota are preserved. Then return to the Home Screen without reopening the app: with the computer awake and Companion running, check that the widget obtains a later sample. iOS decides when to refresh; requesting 30 minutes does not guarantee an exact time.

Keep Companion hidden and check two publications about five minutes apart. Record read and publication times separately. Check that the widget displays sample age and never presents old data as current. Test network loss, restart and first unlock; disconnect and re-pair during a refresh to ensure the previous channel's data does not return.

With the app open, confirm updates arrive without tapping Refresh. Open Privacy and Support: they should use statusline.inmerzion.io in the app language, independently of relay configuration. The Data Plane design and icon should be preserved.

English and Spanish follow the primary system language in the app and widget. Other primary languages use English, even if Spanish is second in the language list.

On your paired iPhone, switch the primary language between Spanish, English and an unsupported language such as French. Reopen Statusline and refresh the widget. Check labels, remaining quota, reset dates, errors, accessibility text and camera permission descriptions. Confirm long Spanish labels fit and pairing/data remain intact.

On a separate clean install, open the local demo and confirm 70% in both the app and widget; paste a `/status` line; pair a real Companion by QR and manual link; refresh from the relay; deny and later grant camera access; disconnect; restart the app and phone; verify no-sample, waiting, synced and error states. Never include QR codes, pairing links or credentials in feedback.
